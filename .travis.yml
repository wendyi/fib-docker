language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t wendyi/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true wendyi/react-test npm test

after_success:
  - docker build -t wendyi/fib-client -f ./client
  - docker build -t wendyi/fib-nginx -f ./nginx
  - docker build -t wendyi/fib-server -f ./server
  - docker build -t wendyi/fib-worker -f ./worker

  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

  - docker push wendyi/fib-client
  - docker push wendyi/fib-nginx
  - docker push wendyi/fib-server
  - docker push wendyi/fib-worker
